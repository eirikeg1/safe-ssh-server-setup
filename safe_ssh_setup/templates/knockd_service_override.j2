# Generated by safe-ssh-setup on {{ timestamp }}
#
# knockd defaults to interface eth0, which matches almost no modern system.
# Resetting ExecStart also sidesteps Debian's /etc/default/knockd gating.

[Service]
ExecStart=
ExecStart=/usr/sbin/knockd -i {{ interface }}
